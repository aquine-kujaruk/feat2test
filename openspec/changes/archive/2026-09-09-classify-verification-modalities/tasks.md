## 1. Inventory and preserve the prior state

- [x] 1.1 Inventory taxonomy references across all project-owned files, including hidden skill resources, ignored authored examples, current specifications, active changes, and archived OpenSpec documents; verify the inventory covers retired tags, existing `@code` expectations, old category-selection prose, and reference links, with immutable evidence and third-party exclusions individually identified.
- [x] 1.2 Preserve the complete prior skill and affected authored files before editing guidance, fixtures, or criteria; verify the snapshot against recorded content hashes and retain Git status so unrelated changes can be preserved during rollback.

## 2. Freeze evaluation inputs and establish the baseline

- [x] 2.1 Revise `dev-model-app.md` to explicitly require real-model verification, retain its source-domain rules, and add a contrasting programmed response-handling fixture; verify their expected modalities are `@ai` and `@code` because of execution evidence, and record the original and revised input identities.
- [x] 2.2 Preserve `dev-mixed-review` as the same agent review exercised directly and through its plugin host, expecting one `@ai` Feature, and add fixtures for combined modality tags, conflicting Rule/scenario tags, and alternative routes without a selection; verify combined or conflicting classifications fail, one selected route is used when established, and an unresolved selection preserves the domain contract without duplication.
- [x] 2.3 Add constructed inputs for custom OpenSpec configuration/schema validation and agent workflow behavior, exact AI output checks, an AI judge of programmed output, unfamiliar instruction packaging, unresolved verification evidence, and known modality with deferred generation details; verify a coverage map connects each case to its source obligations and delta scenarios, including preserved modality when language/framework/file choices change, without implementing a custom workflow or backend.
- [x] 2.4 Reassess all published criteria and corpus metadata under the new modalities, retain existing semantic/language/format/activation coverage, and freeze the comparison corpus and evaluator-only expectations; verify current manifest digests match files, previous identities remain historical, and both guide versions will receive identical raw inputs without expected answers.
- [x] 2.5 Have an independent evaluator retain fresh reserved cases and run the preserved guide on frozen development/regression inputs before instruction edits; verify exact-version resource reads, execution conditions, full outputs, and criterion-level baseline findings are recorded, including contamination limits and any unexecuted run.

## 3. Revise the skill's guidance

- [x] 3.1 Replace the selection reference with `references/verification-modalities.md`, defining mutually exclusive `@code` and `@ai`, inherited Feature scope, unresolved route selection, and checker/packaging independence; verify every requirement in the use-case delta has corresponding guidance and no combined-modality route, artifact-kind fallback, or compatibility alias remains.
- [x] 3.2 Update the entrypoint, agent metadata, domain-discovery reference, and creator/editor briefs to use the behavior-first sequence and link the new reference; verify custom configuration and policy-file objectives fit without a recognized artifact category and all updated links resolve.
- [x] 3.3 Rework the authoring guide and formatting examples around one selected modality per Feature, including the same AI contract across different execution contexts and single modality tags alongside unrelated metadata; verify inherited scope, deferred language/framework/output-layout choices, unaffected use-case rules, meaningful arguments, independent scenarios, and native/Markdown conventions remain intact.
- [x] 3.4 Update the evaluation protocol and rubric to assess execution evidence, the new contrasts, and complete migration; verify wording agrees with the frozen criteria and preserves version isolation, independent assessment, transfer rules, and honest reporting of unexecuted checks.

## 4. Migrate references throughout the project

- [x] 4.1 Sync all three change deltas into the corresponding main Gherkin Craft specifications without archiving this change; verify the four superseded taxonomy requirements are removed, their replacements preserve the mapped scenario coverage, unchanged requirements remain intact, modified requirements match the deltas, and the resulting main specs validate.
- [x] 4.2 Migrate every affected archived proposal, design, task list, and delta, including the archived `specify-executable-assets` change; verify retired taxonomy rules and examples are replaced semantically, each edited archive document carries a retrospective migration note, and past execution is not presented as validation of the revised contract.
- [x] 4.3 Reconcile all remaining inventoried project-owned docs, examples, metadata, fixtures, and active-change references, including links to the renamed guide; verify each inventory entry has a disposition and no authored reference is excluded merely because it is hidden, ignored, or archived.
- [x] 4.4 Check evidence integrity and manifest consistency after migration; verify immutable snapshots, raw runs, and historical observations retain their identities, live input/criteria digests match the updated files, and published cases are not misrepresented as unseen or currently frozen under old identities.

## 5. Validate syntax and observed behavior

- [x] 5.1 Parse affected and newly authored native/Markdown Gherkin examples with the installed parser; verify one selected modality, scenario inheritance, Examples counts and bindings, preserved table values, unrelated metadata, and Markdown tags in separate code spans; verify semantic review rejects combined or conflicting modality tags even when the parser accepts them.
- [x] 5.2 Run the candidate guide on the same frozen development/regression inputs and conditions used for the baseline; verify independent assessment records full-output evidence for every mandatory modality distinction, retained semantic guarantees, and at least one purpose, decomposition, or boundary correction beyond literal retagging.
- [x] 5.3 Obtain an independent editorial review of the complete revised guide, examples, specifications, and migration results; resolve supported findings across all affected occurrences and recheck the whole candidate for at most two correction cycles, verifying that remaining findings are reported rather than silently waived.
- [x] 5.4 Freeze the candidate, compare both versions on independently reserved cases, and perform the existing activation checks in the target host; verify transfer evidence, actual skill-loading observations, published-case provenance, and any unavailable checks are reported separately from output quality.

## 6. Audit completion and report evidence

- [x] 6.1 Repeat the project-wide reference audit after all edits and checks; verify no retired category tags, obsolete `@code` selection rules, acceptance of multiple modalities, or broken guide links remain in authored project material, including archives; verify deliberately invalid combination fixtures expect rejection and every remaining historical-evidence or third-party match has a recorded reason.
- [x] 6.2 Validate this change and affected main specs with OpenSpec, check JSON/manifest integrity, and run existing repository checks appropriate to any changed executable fixtures; verify the final diff stays within the migration, with generation language/framework/file layout still deferred and no new CLI dispatch, adapter, dependency, or custom OpenSpec implementation.
- [x] 6.3 Deliver the comparison and migration report with changed surfaces, inventory dispositions, version identities, source/output evidence, independent-review outcomes, and unexecuted checks; verify required behavioral comparisons have passed before claiming behavioral validation or marking their tasks complete.
