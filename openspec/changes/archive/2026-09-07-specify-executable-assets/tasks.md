## 1. Establish the behavioral baseline

- [x] 1.1 Snapshot the complete working `.agents/skills/gherkin-craft/` before editing, including uncommitted content; verify file hashes and record the snapshot path and execution conditions in the ignored evaluation workspace.
- [x] 1.2 Extend the evaluation manifest, raw cases, and separate expected findings with contextual/missing intent, the same source serving different objectives, all four asset kinds, and a shared contract with two targets; verify coverage against the three delta specs and retain the existing semantic cases and source provenance.
- [x] 1.3 Arrange fresh reserved cases independently of instruction development and treat previously published reserved cases as regressions; verify the manifest distinguishes those roles and candidate inputs exclude expected findings.
- [x] 1.4 Run the preserved guide on development cases in independent contexts; deliver a baseline report identifying purpose, decomposition, boundary, and label results, with version and source identities and any unexecuted checks explicitly recorded.

## 2. Implement coherent modeling guidance

- [x] 2.1 Revise `SKILL.md` and `agents/openai.yaml` around executable intent and the agent/guide distinction; verify identity, language defaults, supported authoring/review modes, and resource links are preserved, and the instructions impose no separate interview.
- [x] 2.2 Update `references/domain-discovery.md` and `references/creator.md` to connect contextual intent, relevant domains, access patterns, and complete Detroit use cases; review the instructions against the intent, decomposition, and internal-workflow scenarios while retaining existing semantic distinctions.
- [x] 2.3 Add `references/verification-targets.md` and route authoring and review to it; verify the four definitions, Feature scope, multiple-target rules, dependency exclusions, pending classifications, and unsupported-scaffolding handling match the use-case delta.
- [x] 2.4 Update `references/gherkin-format.md` with native and Markdown asset-tag placement and a link to the authoritative meanings; parse single- and multiple-tag examples with the existing Gherkin library and verify Feature tags, inherited scenario metadata, bindings, and table data survive.
- [x] 2.5 Revise `references/authoring-guide.md` with contrasting examples for source-plus-objective, separate use cases, software calling a model, and the same contract through skill/plugin boundaries; verify the examples preserve complete observable contracts and the existing argument and language conventions.
- [x] 2.6 Update `references/editor.md` to assess objective fit, responsibility separation, Detroit boundaries, and asset labels; verify its review criteria retain source fidelity, justified coverage, format checks, and review-only scope.

## 3. Demonstrate and preserve the changed behavior

- [x] 3.1 Align `references/evaluation.md`, the rubric, and case criteria with the revised contract; verify they distinguish an observed agent run, metadata parsing, and future target-test execution, and reject a syntactically valid chapter-based output that misses the requested asset use case.
- [x] 3.2 Run the candidate on the frozen development cases under conditions equivalent to the baseline; record criterion-level evidence for all four asset types, contextual intent reuse, missing intent, domain decomposition, multiple targets, and retained semantic guarantees.
- [x] 3.3 Inspect generated native and Markdown artifacts with the installed parser and review their meaning against source and intent; verify relevant tags, expanded examples, data, language, and observable outcomes, reporting parser success separately from semantic success.
- [x] 3.4 Consolidate repeated instructions and resolve supported development findings; if the candidate changes, rerun affected cases and record the resulting version so earlier successful runs are not attributed to an untested revision.

## 4. Validate the complete skill revision

- [x] 4.1 Freeze the candidate and compare it with the preserved guide on the fresh reserved cases; deliver a transfer report identifying versions, outcomes, and limitations, and move any case used to guide further edits into development before a new transfer claim.
- [x] 4.2 Run contextual positive, nearby negative, and explicit-invocation activation checks in the intended host; record observed guide loading and paths separately from output quality, and leave unavailable required checks unverified.
- [x] 4.3 Obtain the independent editorial review required by the existing workflow for the complete revised guide and generated specifications; resolve supported findings within its bounded correction cycle and deliver the final review disposition, disclosing any unavailable independence.
- [x] 4.4 Review the full implementation diff and acceptance report; verify changes stay within the skill/evaluation scope, required new checks pass, retained guarantees have no material regression, and a purpose/decomposition/boundary improvement beyond added labels is evidenced against baseline. Run strict OpenSpec validation and leave this task incomplete if required behavioral evidence is unavailable.

Evidence: [acceptance report](../../../.artifacts/gherkin-craft-eval/specify-executable-assets/acceptance.md).
